export interface Ring {
    id: string
    playgroundsType: string[]
    playgroundsSize: string[]
    contactPerson: string
    contactPhone: string
    doctor: boolean
    shower: boolean
    priceHour: number
    pricePercent: number
    additionally: string
    location: string
    lockerRoom: boolean
}
